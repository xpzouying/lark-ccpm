---
name: "改造 sync.md：Epic Sync 与 Task 创建"
status: open
created: 2026-04-09T10:00:00Z
updated: 2026-04-09T10:00:00Z
depends_on: [001, 002]
parallel: false
conflicts_with: []
---

# Task 003: 改造 sync.md — Epic Sync 与 Task 创建

## Description

改造 `skill/ccpm/references/sync.md` 的前半部分：Epic 同步创建和 Task 记录创建流程。这是整个改造中改动量最大的文件。

## Acceptance Criteria

- [ ] Epic Sync：`gh issue create` → `lark-cli base record create`，返回 record_id 作为 Epic 标识
- [ ] Task 创建：`gh issue create` + `gh sub-issue` → `lark-cli base record create` 并设置"所属 Epic"关联
- [ ] Task 文件不再重命名为外部 ID，保留自增序号，frontmatter 记录 `lark_record`
- [ ] 创建记录时设置正确的字段类型（单选、关联、复选框等）
- [ ] 依赖关系和并行标记写入多维表格对应字段

## Technical Details

- 命令映射见 SPEC 第 3.1 节
- 需从 `.claude/lark-ccpm.yml` 读取 app_token 和 table_id
- record create 返回的 record_id 需回写到 task 文件 frontmatter

## Effort Estimate

- Size: L
- Hours: 6
