---
name: "改造 init.sh：lark-cli + glab 初始化"
status: open
created: 2026-04-09T10:00:00Z
updated: 2026-04-09T10:00:00Z
depends_on: []
parallel: false
conflicts_with: []
---

# Task 001: 改造 init.sh — lark-cli + glab 初始化

## Description

重写 `skill/ccpm/references/scripts/init.sh`，将 GitHub 依赖替换为飞书多维表格 + GitLab 初始化逻辑。

## Acceptance Criteria

- [ ] 检查 `lark-cli` 是否安装及认证状态
- [ ] 检查 `glab` 是否安装及认证状态（`glab auth status`）
- [ ] 移除 `gh-sub-issue` 扩展检查
- [ ] 交互式获取或自动创建飞书多维表格（含标准字段和字段类型）
- [ ] 获取 GitLab 项目路径
- [ ] 生成 `.claude/lark-ccpm.yml` 配置文件
- [ ] 连通性验证：读取多维表格、访问 GitLab 项目
- [ ] 仓库 remote 检查改为 GitLab URL

## Technical Details

- 替换 `gh auth status` → `lark-cli` 认证检查 + `glab auth status`
- 替换 GitHub label 创建 → `lark-cli base` 创建表/字段/视图
- 多维表格字段定义见 SPEC 第 2.1 节（15 个字段）

## Effort Estimate

- Size: L
- Hours: 6
