---
name: "改造 conventions.md：Frontmatter 与命令约定"
status: open
created: 2026-04-09T10:00:00Z
updated: 2026-04-09T10:00:00Z
depends_on: []
parallel: true
conflicts_with: []
---

# Task 002: 改造 conventions.md — Frontmatter 与命令约定

## Description

更新 `skill/ccpm/references/conventions.md`，将 Frontmatter schema、GitHub Operations、认证检查全部替换为飞书多维表格 + GitLab 版本。

## Acceptance Criteria

- [ ] `github:` 字段替换为 `lark_record:` + `gitlab_mr:`
- [ ] Epic frontmatter 新增 `lark_app:` 和 `lark_table:` 字段
- [ ] "GitHub Operations" 整节替换为 "Lark Operations" + "GitLab Operations"
- [ ] 认证检查从 `gh auth login` 改为 `lark-cli` + `glab auth login`
- [ ] Issue 编号获取改为从 frontmatter 读取 `lark_record:` 字段
- [ ] Git 约定中 push 目标改为 GitLab remote
- [ ] 配置文件 `.claude/lark-ccpm.yml` 的读取约定

## Technical Details

- 这是所有后续改造的"约定基础"，其他文件的改造需遵循此处定义的 schema
- Frontmatter schema 详见 SPEC 第 5 节

## Effort Estimate

- Size: L
- Hours: 4
