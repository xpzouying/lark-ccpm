---
name: "改造 epic-*.sh 脚本"
status: open
created: 2026-04-09T10:00:00Z
updated: 2026-04-09T10:00:00Z
depends_on: [002]
parallel: true
conflicts_with: []
---

# Task 008: 改造 epic-*.sh 脚本

## Description

更新 `skill/ccpm/references/scripts/` 下的 `epic-list.sh`、`epic-show.sh`、`epic-status.sh`，将 frontmatter 字段读取从 `github:` 改为 `lark_record:` 和 `gitlab_mr:`。

## Acceptance Criteria

- [ ] `epic-list.sh`: `github:` 字段读取改为 `lark_record:`
- [ ] `epic-show.sh`: `github:` 字段读取改为 `lark_record:` + `gitlab_mr:`
- [ ] `epic-status.sh`: `github:` 字段读取改为 `lark_record:`
- [ ] 其余脚本（blocked.sh、next.sh、status.sh 等）检查是否有 `github:` 引用，如有则更新
- [ ] 所有脚本能正确读取新 frontmatter 字段

## Effort Estimate

- Size: M
- Hours: 2
